<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppControlePro">

        <activity
            android:name=".activities.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activities.MainActivity" android:exported="false" />
        <activity android:name=".activities.CadastroUsuarioActivity" android:exported="false" />
        <activity android:name=".activities.TipoUsuarioActivity" android:exported="false" />
        <activity android:name=".activities.CadastroTimeActivity" android:exported="false" />
        <activity android:name=".activities.EscolherTimeActivity" android:exported="false" />
        <activity android:name=".activities.JogadoresActivity" android:exported="false" />
        <activity android:name=".activities.JogosActivity" android:exported="false" />
        <activity android:name=".activities.FinanceiroActivity" android:exported="false" />

    </application>

</manifest>